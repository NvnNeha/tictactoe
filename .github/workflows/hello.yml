name: Hello World

on:
  push:
    branches:
      - main
    paths:
      - 'index.html'

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello World"
